#!/usr/bin/env {{ESCRIPT}}
%%% vim:sw=2:ts=2:et
%%! -env ERL_CRASH_DUMP /dev/null {{FIXDUMP_ENV}}
%%%-----------------------------------------------------------------------------
%%% @doc FIX log file parser
%%%
%%% Parses a FIX log file produced by fix_logger and outputs it in a human
%%% readable format.
%%%
%%% @author Serge Aleynikov <saleyn@gmail.com>
%%%-----------------------------------------------------------------------------

%-mode(debug).
-mode(compile).

main(Args) ->
  fixdump:main(Args).
