%{include_app_messages, ['Logon', 'AllocationInstruction']}.

%% For each message define optional fields that are added to the required
%% section of a message
{mandatory, #{
  header  => ['PossDupFlag', 'PossResend', 'SendingTime', 'LastMsgSeqNumProcessed']
}}.
